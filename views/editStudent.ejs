<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit Student</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/style.css" />
  </head>

  <body>
    <!-- NAVBAR -->
    <nav class="navbar navbar-dark navbar-custom">
      <div class="container">
        <a class="navbar-brand fw-bold" href="/">Student List App</a>
      </div>
    </nav>

    <!-- FORM CARD -->
    <div class="container mt-5">
      <div class="card-custom">
        <h2 class="text-center fw-bold mb-4">Edit Student</h2>

        <form
          method="POST"
          action="/editStudent/<%= student.studentId %>"
        >
          <label class="form-label">Student Name</label>
          <input
            type="text"
            name="name"
            class="form-control mb-3"
            value="<%= student.name %>"
            required
          />

          <label class="form-label">Date of Birth</label>
          <input
            type="date"
            name="dob"
            class="form-control mb-3"
            value="<%= student.dob %>"
            required
          />

          <label class="form-label">Contact Number</label>
          <input
            type="text"
            name="contact"
            class="form-control mb-4"
            value="<%= student.contact %>"
            required
          />

          <button class="btn btn-custom w-100">Update Student</button>
        </form>
      </div>
    </div>

    <footer class="footer">Student App © 2026</footer>
  </body>
</html>
